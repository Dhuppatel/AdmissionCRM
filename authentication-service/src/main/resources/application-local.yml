spring:
  datasource:
    url: ${DB_URL_local:jdbc:mysql://localhost:3306/admissioncrm_auth?useSSL=false&serverTimezone=UTC}
    username: ${DB_USERNAME_local:admissionCRM}
    password: ${DB_PASSWORD_local:Admission123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  config:
    import: optional:file:./authentication-service/.env[.properties]
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

lead-service:
  base-url: http://localhost:8080/api/leads